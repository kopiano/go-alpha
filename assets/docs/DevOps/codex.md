# codex

## codex cli
* install
```shell
$ npm install -g codex  # node
$ brew install codex    # macos
```
* usage
```shell
$ codex
$ /exit   # 退出codex
```
* 使用中转站接入gpt5.5
- cc switch：自定义模型填入API Token和接口网址，关闭路由
- 然后就可以直接使用codex啦
- 中转站如：klapi.site